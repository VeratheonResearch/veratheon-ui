# Supabase Configuration for Frontend
# These variables are prefixed with VITE_ so they're exposed to the browser

# Supabase URL (local development example)
VITE_SUPABASE_URL=http://127.0.0.1:54321

# Supabase Anonymous/Publishable Key
# Get this from: supabase status
# Or from Supabase Dashboard -> Settings -> API
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API URL
# Local development (outside Docker): http://localhost:8085
# Docker Compose: Overridden in docker-compose.yml to http://veratheon-research:8085
# Production (Vercel): https://veratheon-research.fly.dev
VITE_API_URL=http://localhost:8085

# Server-side Supabase Configuration (no VITE_ prefix)
# These are used by server-side API endpoints
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
